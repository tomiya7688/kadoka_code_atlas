name: Contract Fixture
on: [push, pull_request]
jobs:
  test:
    steps:
      - name: Tests
        run: python -m pytest tests/unit/test_service.py
  build:
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build
        run: python -m build
